export const STORY_SEEN_URL = '*://*.instagram.com/api/v1/stories/reel/seen*';
export const UNIQUE_RULE_ID = 1;

export enum StorySeenActions {
  BLOCK = 'block',
  ALLOW = 'allow',
}

export enum BadgeColors {
  ENABLED = '#0097ff',
  DISABLED = '#777',
}

export enum BadgeText {
  ON = 'On',
  OFF = 'Off',
}
